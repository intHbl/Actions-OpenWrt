#!/bin/bash

(

    cd "$(dirname "$0")"
    
    if [ -z "$1" ];then
        echo "[E] need arg1"
        echo "   usage $0 <dirname>"
        echo "     arg1 == 'config_<xxx>'"
        exit 1
    fi

    if ! [ -d "$1" ];then
        echo "[E] need arg1 is a dir"
        echo "   usage $0 <dirname>"
        exit 2
    fi

    workflowsDir=".github/workflows"
    if [ -e "$workflowsDir" ];then
        if ! [ -d "$workflowsDir" ];then
            exit 3
        fi
    else
        mkdir -p "$workflowsDir"
    fi

    


    dir="$(basename "$1")"
    FIRMWARE="$(echo "$dir" | sed "s/^config_//")"


    flag_small=false
    if [ -f "$dir"/compile/feeds.conf/feeds.conf.default.small ];then
        flag_small=true
    fi

    for f in "$dir"/compile/config/models/*.conf;do

        if [ ! -f "$f" ];then
            continue
        fi
        NAME="$(basename "$f")"
        NAME="$(echo "$NAME" |sed "s/.conf$//g")"
        echo "[Info] NAME=$NAME , conf=$f"

        after_clone_sh="false"
        if [ -e "$f.dts_gen" ];then
            after_clone_sh="$f.dts_gen"
            after_clone_sh="$(echo "$after_clone_sh" | sed 's/\//\\\//g')"
        fi

        yml="$workflowsDir/${FIRMWARE}_${NAME}.yml"
        {
            echo "# generated by $0"
            sed "s/<<<TODONAME>>>/${FIRMWARE}_${NAME}/g" action/TEMPLATE@openwrt.yml \
                | sed "s/<<<TODODIR>>>/$dir/g" \
                | sed "s/<<<TODOMODEL_NAME>>>/$NAME/g" \
                | sed "s/<<<TODOFLAG_SMALL>>>/false/g" \
                | sed "s/<<<TODOAFTER_CLONE_SH>>>/$after_clone_sh/g"
        } > "$yml"

        if $flag_small;then
            for s in "$dir"/compile/config/small.build.luci-app.conf.small*;do
                if ! [ -f "$s" ];then
                    continue
                fi
                small="$(echo "$s" | grep -Eo "small[0-9]*$")"
                yml="$workflowsDir/${FIRMWARE}_${NAME}__with_${small}.yml"
                {
                    echo "# generated by $0"
                    sed "s/<<<TODONAME>>>/${FIRMWARE}_${NAME}__with_${small}/g" action/TEMPLATE@openwrt.yml \
                        | sed "s/<<<TODODIR>>>/$dir/g" \
                        | sed "s/<<<TODOMODEL_NAME>>>/$NAME/g" \
                        | sed "s/<<<TODOFLAG_SMALL>>>/$small/g" \
                        | sed "s/<<<TODOAFTER_CLONE_SH>>>/$after_clone_sh/g"
                } > "$yml"
            done
        fi
        # if [ "$conf_name" == "mir3g_v2.conf" ] || [ "$conf_name" == "mir4a_GB.conf" ];then

        # fi

    done

)

# __with_small

